# Legacy Result Schema

The files below this directory predate correctness remediation schema version 2.
They use observation-count walk-forward windows, restarted window capital, and
non-causal regime thresholds. They are retained only as historical regression
artifacts and must not be interpreted as current research results.

Current research experiments are written under `results/research_v2/`.
